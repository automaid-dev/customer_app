<?xml version="1.0" encoding="utf-8"?>
<!-- Same reasoning as the default drawable/launch_background.xml —
     kept in sync so dark-mode/API21+ devices get the identical
     seamless background instead of falling back to the system
     default color. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_color" />
</layer-list>
